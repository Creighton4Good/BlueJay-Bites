spring.application.name=bluejaybites
spring.datasource.url=jdbc:mysql://localhost:3306/bluejaybites
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always

server.port=8080

management.endpoints.web.exposure.include=*
# TODO: narrow actuator endpoints for production
endpoint.health.show-details=always
spring.boot.admin.context-path=/admin-dashboard

spring.boot.admin.client.url=http://localhost:8080/admin-dashboard
info.env.enabled=true

# Enable related features.
spring.cloud.azure.active-directory.enabled=true
# Specifies your Microsoft Entra ID tenant ID:
spring.cloud.azure.active-directory.profile.tenant-id=${TENANT_ID}
# Specifies your App Registration's Application ID:
spring.cloud.azure.active-directory.credential.client-id=${CLIENT_ID}
# Specifies your App Registration's secret key:
spring.cloud.azure.active-directory.credential.client-secret=${CLIENT_SECRET}

spring.config.import=optional:file:.env[.properties]
# Comma-separated list of emails that should be provisioned as admin on first login.
app.admin-emails=

# Photo upload limits and storage location
spring.servlet.multipart.max-file-size=2MB
spring.servlet.multipart.max-request-size=2MB

# S3 bucket for event photos. Container storage is not durable.
app.photo-bucket=${PHOTO_BUCKET:bjbites-staging-photos}
app.aws-region=${AWS_REGION:us-east-2}


# The load balancer terminates TLS and forwards plain HTTP to the container, so
# without this Spring builds redirect URIs with http and Entra rejects them.
# Trusting the forwarded headers makes it use the original https scheme.
server.forward-headers-strategy=framework

# Set the redirect URI explicitly. Behind the load balancer the derived value
# was missing the registration id on the end, which Entra rejects.
spring.cloud.azure.active-directory.redirect-uri-template={baseUrl}/login/oauth2/code/azure
